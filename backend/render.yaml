services:
  - type: web
    name: hos-backend
    env: node
    buildCommand: npm install
    startCommand: node railway-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
